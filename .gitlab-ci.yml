image: python:3.7-stretch
stages:
  - lint
  - test
before_script:
  - pip install pipenv
  - ./run.sh init

# Any branch
lint:
  stage: lint
  script:
    - ./run.sh lint

test:
  stage: test
  script:
    - ./run.sh test
